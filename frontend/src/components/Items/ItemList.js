import React from 'react';

const ItemList = () => {
    return (
        <div>
            <h2>Item List</h2>
            {/* Fetch and display the list of items here */}
        </div>
    );
};

export default ItemList;
